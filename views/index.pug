<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title GET NEW WORD
    body
        h1 GET RANDOM WORD
        form(action="/random", method="get")
            button(type="submit") Random word
